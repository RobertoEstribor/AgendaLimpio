<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--<style name="Theme.PruebasResponseAndroid" parent="android:Theme.Material.Light.NoActionBar" /> -->

    <style name="Theme.AgendaLimpio" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/screen_background</item>
        <item name="materialButtonStyle">@style/App.Button.Primary</item>
    </style>

    <!-- Estilo personalizado para los botones principales de la app -->
    <style name="App.Button.Primary" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/btnColor</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.Button</item>
    </style>

    <!-- Estilo para la forma de los botones (bordes redondeados) -->
    <style name="ShapeAppearance.App.Button" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Tema para la Splash Screen (Android 12+) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Color de fondo de la splash screen -->
        <item name="windowSplashScreenBackground">@color/brand_primary</item>
        <!-- Icono a mostrar en la splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <!-- Tema a aplicar después de la splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.AgendaLimpio</item>
    </style>

    <style name="MiEstiloDeSpinner" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">#4CFFFFFF</item>
    </style>

    <style name="MenuButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">120dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">24dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>